using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LacoRepeticao : MonoBehaviour
{
    // for é usado quando sabe a quantidade de vezes que precisa repetir
    //while ou dowhile é utilizado quando não sabe a quantidade de vezes a se repetir
    // Foreach usado em listas de arrays para executar uma operação
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
